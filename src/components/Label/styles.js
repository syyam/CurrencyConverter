import { StyleSheet, Dimensions } from 'react-native'

const styles = StyleSheet.create({

    labelText: {
        width:"20%",
        textAlign:"center"
        // color: "white"
    },

});

export default styles;
