import { StyleSheet, Dimensions } from 'react-native'

const styles = StyleSheet.create({

    labelText: {
        width:"20%",
        alignItems: 'center',
        alignSelf: 'center',
        textAlign:"center"
        // color: "white"
    },

});

export default styles;
